#include "App.hpp"

void App::Render() {
    /*SDL_RenderClear(m_Renderer); // 清除當前渲染器內容
    SDL_Rect dstRect = {static_cast<int>(m_man.GetPosition().x), static_cast<int>(m_man.GetPosition().y), 50, 50}; // 設定圖片顯示的位置和大小
    SDL_RenderCopy(m_Renderer, m_CurrentTexture, nullptr, &dstRect); // 將當前圖片渲染到指定位置
    SDL_RenderPresent(m_Renderer); // 呈現渲染結果*/
}